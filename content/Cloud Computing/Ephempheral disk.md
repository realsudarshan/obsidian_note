## **Ephemeral (scratch) storage** ⚡

- Temporary space that exists **only while the VM is running**.
    
- Example: local SSD or temporary disks for caching/swap files.
    
- Once VM stops/deletes, the data is gone.
    
- Good for **high-speed caching** or **short-term computations**.